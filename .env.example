# ── SphinxAI ──────────────────────────────────
# Clave de API para autenticación con SphinxAI.
# Se pasa al SDK como parámetro explícito O como env var SPHINX_API_KEY
# según la interfaz detectada (ver main.py run_sphinxai).
SPHINX_API_KEY=

# ── Límites ───────────────────────────────────
# Máximo de filas aceptadas en una sola petición /analyze.
MAX_ROWS=5000

# ── CORS ──────────────────────────────────────
# Orígenes permitidos, separados por coma.
# En producción restringir a tu dominio Lovable, ej:
#   CORS_ORIGINS=https://tu-app.lovable.app
# En desarrollo dejar "*" para permitir todo.
CORS_ORIGINS=*
